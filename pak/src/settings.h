#ifndef __BATTERYCARE_SETTINGS_H__
#define __BATTERYCARE_SETTINGS_H__

#include <SDL2/SDL.h>

/* Runs the Battery Care menu until the user presses B. Returns when done. */
void SettingsModule_run(SDL_Surface *screen);

#endif
